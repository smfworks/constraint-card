export function Header() {
  return (
    <header className="mast">
      <div className="mast-brand">
        <span className="mark" aria-hidden="true" />
        <div>
          <p className="eyebrow">SMF Works · Human-AI lab</p>
          <h1>Constraint Card</h1>
        </div>
      </div>
      <p className="lede">
        Paste MUST / MUST NOT / STOP rules. Print a standing constitution for
        the agent. Share the bound — not enforcement.
      </p>
    </header>
  );
}
